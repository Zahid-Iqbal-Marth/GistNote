import React from "react"
import "./Loading.css"


const Loading : (props: object) => JSX.Element = (props: object) => {
    return (
        <section id="loading-case" className="loading-position ">
              <section className="loading-icon">
              </section>
            </section>
    )
}

export default Loading;